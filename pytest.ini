[pytest]
testpaths = tests
markers =
    unit: fast, deterministic tests on synthetic in-memory data
    golden: characterization tests comparing against committed reference outputs
    slow: heavy tests run only with --runslow (e.g. full ExampleData)
addopts = -ra
